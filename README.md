# SpotifyConnectLocal

Script to authenticate with Spotify locally and build playlists from a song list.

## Setup

1. Create a `.env` with your Spotify credentials.
2. Install dependencies.
3. Add songs to `songs.txt`.
4. Run `main.py`.
